Search Results
search: {{ search.terms }}
{% if search.results_count > 0 %}
	<ol>
		{% for item in search.results %}    
			<li><a href="{{ item.url }}">{{ item.title }} &mdash; {{ item.content | strip_html | truncatewords: 10 }}</a></li>
		{% endfor %}
	</ol>
{% else %}
	No results found.
{% endif %}